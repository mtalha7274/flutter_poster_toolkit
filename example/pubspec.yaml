name: flutter_poster_example
description: Example app for the flutter_poster package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  flutter_poster:
    path: ..
  image_gallery_saver_plus: ^4.0.1
  image_picker: ^1.2.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
