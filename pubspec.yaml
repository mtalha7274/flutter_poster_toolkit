name: flutter_poster
description: A reusable Flutter poster editor package with editable text, image, and shape elements.
version: 0.1.0
homepage:

environment:
  sdk: ^3.10.3
  flutter: ">=3.38.0"

dependencies:
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  flutter_colorpicker: ^1.1.0
  google_fonts: ^8.0.2
  screenshot: ^3.0.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
